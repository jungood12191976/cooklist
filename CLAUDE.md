# KING THE QUEST（cooklist）— 開発ガイド

このファイルはこのリポジトリを開くすべてのセッションで自動的に読み込まれる。
プロジェクトの背景・絶対厳守のルール・アプリの現状は、Claudeが作業を始める前に必ずここを読むこと。

## アプリの説明

「KING THE QUEST（キング・ザ・クエスト）」は、ユーザー（以下「キング」）が今後2年間本気で使い倒すための個人用アプリ（販売目的ではない）。

- **本体**: 単一HTMLファイル `index.html`（約21〜27MB、画像・音声はbase64埋め込み）
- **公開URL**: https://jungood12191976.github.io/cooklist/
- **デプロイ**: `main`ブランチに`index.html`をpush → GitHub Pagesが自動反映
- **バージョン管理**: `APP_BUILD`定数（例`2026-09-19i`）を画面下部に表示し、キャッシュ判別に使う

### 最重要方針（アプリの世界観）

このアプリは「勉強アプリ」や「情報ツール」にしてはいけない。常にRPG・冒険の世界観を崩さないこと。文字だらけ・解説だらけ・勉強チックな見た目や表現になった瞬間、キングは使わなくなる可能性が高い。どんなに機能が優秀でも「続けられないアプリ」は失敗である。

本質的な目的：
- 自分の成長を「経験値」として可視化し、蓄積していくこと
- 特に日本株トレードにおいて、実践の中で得た経験・仮説・気づき・結果を記録し、経験値として積み上げていくこと
- 遺伝子情報、英語、天気、女性ホルモンなども、すべて「冒険の要素」として扱う
- 表面的な便利機能ではなく、本気で利益を取れる力を少しずつ育てるための仕組みを作ること

トレードについて：キングは教科書的な勉強よりも、実践で売買を繰り返す中で学んできた。cisさんのように「毎日チャートを見続け、仮説を立て、経験を蓄積する」ことを重視している。単なるシグナル表示だけでなく、トレード結果のフィードバック・仮説と結果の記録・経験の可視化が将来的に非常に重要（本丸のシステムはまだ未着手）。

## Claudeへの行動指針

1. 常にRPG・ゲームの世界観を優先して提案すること
2. 機能を追加・修正するときも「冒険感・成長感・達成感」が薄れないかを最優先で考えること
3. 文字が多くなりそうな場合は、必ず「どうすればゲームっぽく見せられるか」を一緒に考えること
4. 大きな変更の前には必ずキングに確認すること
5. 回答は簡潔に、要点を優先すること

## 絶対厳守の禁止事項

- **断定した虚偽案内の禁止（最重要）**：確認していないこと（アプリの画面構成、メニューの場所、操作手順、ボタン名など）を、確認したかのように断定して案内することを絶対禁止。分からないことは「分からない」と明言し、推測で手順を作らない。提案の前に、公式ドキュメント・実際のコード・実際のAPIレスポンスなど確認可能な情報源で裏を取ってから言う
- **実装前のデータ確認義務**：実装・修正の前に、必ず実物のデータ（実際のAPIレスポンス、実際のページのHTML、実際の画面表示など）を取得・確認してからコードを書く。想像や推測で正規表現・パーサー・仕様を書くことを禁止。実装後も実データでダブルチェックしてから「できました」と報告する
- **勝手に重要事項を決めることの禁止**：監視対象銘柄・注目銘柄・判定条件・重要な数値パラメータなど、投資判断に関わる項目をクロードが勝手に決めることを禁止。必ずキングに確認してから決定する。初期値・デフォルト値を設定する場合も、キングに提示して承認を得てから実装する
- **UI変更の事前提案義務**：タブ構成・表示位置・画面レイアウトなど、UIに関わる変更は実装前に必ず提案してキングの確認を得る。「作ってから見せる」ことを禁止
- **GitHub連携トラブル時の対応**：git push・GitHub APIアクセスが失敗した場合、1回の失敗で即座に「権限がありません」と結論づけることを禁止。最低3回はリトライ・別角度からの確認を行ってから、「以前は繋がっていたが、認可が更新されているようなので繋ぎ直しが必要」という言い方で伝える。技術的に確定した限界に達した場合は、それ以上推測で回避策を作らず、確定した事実だけを伝える
- **キングの手作業を最小化する義務**：提案する前に、クロード自身が実際にできる範囲かどうかを検証してから提案する。キングに回りくどい手作業をさせる手順を可能な限り避け、技術力で最短ルートを自分で考えて実行する。どうしても手作業が必要な場合は「今回限りか、今後も毎回必要か」を明確に伝える
- **数値・技術情報の正確性**：数値を使う前に、会話内で既に確認済みの値をまず書き出す（記憶から再入力しない）。計算は必ず検証する。矛盾があれば自分で解決し、キングに二重チェックさせない
- **認証情報をリポジトリに置かない**：このリポジトリはGitHub Pagesで一般公開されている。トークン・APIキー・PAT等の秘密情報を、コード・Markdown・コミットメッセージいずれにも絶対に書き込まない。GitHub書き込みはセッション側に認可された仕組みで行われ、トークンの手入力・保存は不要（下記参照）

## GitHub連携について（重要）

- 標準デプロイ手順：Claude Code環境からの`git push`（このセッションのようにセッション自体がリポジトリへの書き込み権限を持つ場合）を使う
- **過去に問題になったPAT方式は使わない**：以前は`curl`でGitHub PAT（個人アクセストークン）をコマンドに埋め込みContents APIを叩く方式を使っていたが、この方式は**トークン管理の手間とセキュリティリスクを生む**ため廃止した。トークンをキングに再入力させることは絶対にしないこと
- push・GitHub APIアクセスが失敗した場合は上記「GitHub連携トラブル時の対応」に従う

## 「金の鉱山」機能（投資支援の中核）

日本株の値動きスキャナー。8層の市場材料フレームワークに基づく：

①需給・ガバナンス（信用買い残・大株主異動・自己株買い・株式分割）
②業績・ファンダメンタルズ（決算・ガイダンス・アナリスト評価）
③訴訟・ガバナンスリスク（適時開示・係争）
④セクター地合い（海外同業決算・半導体指数・NAND/DRAM市況）
⑤マクロ（為替・金利・VIX・原油・日経VI）
⑥テクニカル（株価・出来高・RSI・移動平均）
⑦センチメント・解釈（強気/弱気の分かれ方）
⑧要人発言・戦略的リーク（トランプ発言・FRB高官・AI企業CEO発言・匿名リーク報道。市場心理を誘導する目的で戦略的に使われることがあるという前提を採用）

実装状況：
- ①⑥＝5条件判定＋点火前シグナルとして実装済み
- ③＝適時開示ウォッチとして実装済み
- ②④⑤⑧＝`research-report.md`で情報源は調査済みだが、アプリへの実装はまだ
- ⑦（センチメント）＝無料で信頼できる自動収集手段が見つからず「実装不可」と結論済み（みんかぶ403、Yahoo掲示板は方針上除外、StockTwitsは日本株カバレッジ薄、松井証券は会員限定）

### UI構成：3タブ

1. **日経225タブ**：日経225構成銘柄（225銘柄）を対象に、急騰・ギャップアップ・出来高急増・高値ブレイク・ゴールデンクロスの5条件で機械的に判定
2. **売買代金上位タブ**：Yahoo Financeの売買代金ランキング上位120銘柄＋日経225銘柄を対象
3. **適時開示タブ**：TDnet（やのしんWEB-API `webapi.yanoshin.jp` 経由）から、指定した監視銘柄の開示情報を取得し需給/業績/リスク/事業の4層に自動分類。銘柄はアプリ上で自由に追加・削除可能（最大20銘柄、`wd3:tdnetwatch`キーに保存）。**この監視銘柄は当初クロードが勝手に選定してキングに叱責され、編集可能化した経緯がある**

「スタート」1つで3処理（ランキング取得・銘柄スキャン・適時開示取得）を自動一括実行（`runGoldMineAll()`）。実行開始時のモードを`modeAtStart`で固定し、実行中にタブ切替しても結果が混ざらない。

### 「点火まで秒読み」機能

CTA（トレンドフォロー型ファンド）のエントリー条件が金の鉱山の既存5条件とほぼ一致することから、「まだ条件が成立する前の状態を見せる」機能。SG Trend Indicator（20日/120日移動平均クロス、実CTA挙動と相関0.75）を根拠に、`evaluateApproachTags`関数で以下を検出：
- 短期GC接近：5日線が25日線を抜けるまであと何営業日か
- 中期GC接近（CTA帯）：20日線と120日線の交差まであと何営業日か
- 高値ブレイク直前：直近20日高値まであと2%以内
- 出来高が増え始めた：20日平均の1.3〜2.0倍

状態変数`goldMineApproach`、判定関数`evaluateApproachTags`、保存キーは`wd3:goldmine`内の`approach`。

### 主要な内部データ

- `NIKKEI225`配列（225銘柄コード。誤コード`6974`は`6976`太陽誘電に修正済み）
- `STOCK_NAMES_JA`辞書（225銘柄の日本語社名。出典：日本経済新聞社公式「日経平均株価 構成銘柄一覧」、2026-09-19取得）
- `tdnetWatch`配列（適時開示の監視銘柄、初期値285A/5803/6857/8035/6146、編集可能）

### ランキング・株価取得の仕組み

- Yahoo Finance優先。プロキシ経路：自前Cloudflare Worker（king-stock-proxy.jungood1219.workers.dev）→ jina（r.jina.ai）→ allorigins → codetabs
- corsproxyは401（有料化）で廃止。stooqはJSチャレンジで機械的取得不能のため除外
- 取得期間は1年（120日移動平均計算に必要）。429検出あり。「接続テスト」ボタンでトヨタ7203を全経路診断

### 名前解決ロジック（`nameFor(code)`）

1. ランキング由来の日本語名（`stockNames`、APIの`meta.shortName`からも取得）
2. アプリ内蔵の日経225日本語名（`STOCK_NAMES_JA`）
3. 株価APIの英語名（フォールバック）

※この仕組みに至るまで3回の実装ミスがあった。「実装前に必ず実物データを確認する」ルールはこの反省から生まれた。

## 3画面の役割分担とUI方針転換（2026-09-21夜〜22朝に別スレッドで合意・確定済み設計）

**重要**：以下は以前ナレッジドキュメントに反映されておらず、2026-09-22にキングが会話履歴から掘り起こして提供した内容。すでに合意・確定している方針なので、実装のたびに再確認を求めないこと。

### 「気配察知」へのUI主従転換（金の鉱山）
- 現在メインの「🔥もう点火した銘柄」（5条件＝結果的に後追い）と、おまけ扱いだった「🔥点火まで秒読み」の**主従を逆転させる**
- 新しい見出し：🔮 **気配察知**（メイン・上段）／⚔️ すでに動いた敵（下段・答え合わせ用に降格）
- 「点火まで秒読み」の各シグナルにRPG的な呼び名を付ける：短期GC接近→「魔物の気配（弱）」／中期GC接近（CTA帯）→「大物の気配（強）」／高値ブレイク直前→「壁が軋み始めた」／出来高増え始め→「足音が近づいている」。複数重なるほど「強い予兆」として見せる

### 適時開示の監視銘柄を「仲間に加える」ボタン化
- 銘柄コード手入力の動線を廃止し、スキャン結果カードに「👥仲間に加える」ボタンを設置。押すと自動でtdnetWatch配列に追加（入力ゼロ）
- 監視中銘柄は「パーティ」画面に一覧表示。外すときはドラッグまたはワンタップで「離脱」
- キング評価：「すごくいいです、いきなりRPG感が出てきました」→ 採用確定

### マクロ層（⑤要人発言含む）を「天気」機能に統合
- 既存の天気機能を拡張し、マクロ環境を天気アイコンに変換：VIX高・SOX急落→「⛈️嵐（波乱の日）」／半導体指数が強い→「🌤️順風」／Polymarket政治指標が🔴→「🌪️竜巻警報（要人発言に注意）」／快晴＝穏やか
- ゲーム的演出を明確に希望（「今は意外に目立たない感じの雨や太陽」との不満あり）：嵐＝画面端に雨粒＋時々雷光／順風＝勇者の後ろにスピード線／竜巻警報＝画面シェイク＋警報音／快晴＝光の粒子がキラキラ舞う
- 「なぜこの天気か」を説明する**賢者（占い師）モーダル**を追加：生の数値を並べず、1項目1行のセリフ形式で見せる（例：「⚡半導体の風が強く吹いておる（SOX +3.1%）」）。キング評価：「すごくいいです！私が求めてるのはこういう感じです」→ 方向性確定

### 3画面の役割分担（確定・最重要）
- **🧑‍🌾 勇者画面＝「今日の仮説を立てる場所」（ミクロ・凝縮）**：朝8:30ブリーフの要点をここに統合（キング「スマホでPC画面を見るのが面倒で読まなくなっていた」との背景あり）。「📜使者からの伝令」という手紙型UIコンポーネントで、気配察知件数・天気・速報を1枚に凝縮表示し、詳細はタップで展開する
- **🏰 天空の城＝「マクロ・振り返りの場所」（事実として詳細に提示）**：以前「まだ実装しなくていい」だった冒険日誌案（レーダーチャート＋クエストログ）を**実装対象に戻すことで合意済み**（再確認不要）。盛り込む内容：実際に起きたこと（値動きの結果）／仮説と結果の答え合わせ（フィードバックループ機能と直結）／決まっているイベント情報（TOPIX・MSCI・Nikkei225の入れ替え、決算発表予定日等）を正確に取得し分かりやすく表示／8層バランスのレーダーチャート。レーダーチャートは「ただの味気ないチャートじゃないか」との懸念から**盾型・光るエフェクトで見せる方針で合意**（詳細デザインは未定）
- **⏰ 時のへや＝「過去問道場」（英語ではなく株の疑似体験学習の場に方針転換済み）**：当初は英語機能を予定していたが、キングが違和感を表明し方針転換。合意内容：「金の鉱山」で拾った過去の速報・材料と、その前後の値動きを**1問1画面のクイズ形式**で出題。**手動運用は明確に拒否**（「手動は基本やらないようにしたい。忘れたらもうその機能はずっと使わなくなる」）→ 速報ログから自動で問題化できる仕組みができるまで実装を待つ（現状はプレースホルダーの「準備中」説明文のみ、これは意図通りの状態）。**解説の出し方に重要な指定あり**：「これは〇〇のせいで下がりました」のようなアナリスト風の断定的な因果説明は禁止。代わりに、結果の前後で指数・ニュースがどう数値的に変化したかを**事実の一覧**として見せ、原因は**「仮説リスト」**として提示する（本当に特定できる場合のみ回答として教える）
- **勇者画面と天空の城は「アクティブな材料リスト」という共通データ構造を参照する設計で確定**：キング「仮説はちゃんと仮説を立てられるキーワードがしっかりわかりやすく提示されていること、天空の城では事実として詳細に情報を提示していること。2つの場面は表現の仕方は違うが、ちゃんとつながっていないと『仮説にはそんなキーワードなかったけど、天空の城の細かい説明ではあった』だと『早く言ってよ』的に落胆する」という明確な要求から。勇者画面（要約・仮説向け）と天空の城（詳細・事実向け）が必ず同じ材料データを参照すること
- **後回し確定**：信用取引をしている人の心理・タイミング・ルールを疑似体験するシミュレーションゲーム要素（時のへやのB案）は、過去問道場（A案）の後に着手

### 注意：「英語（魔法の言葉）」機能タスクとの関係
上記の方針転換で、時のへやは英語練習の場ではなくなった。ただし「王様と話す」ボタン（`openMagicWordsModal`、天空の城とは無関係のランダムポップアップ「王国の使者が現れた」から呼ばれる、王様との謁見＝15分間英会話の練習）は既存の別機能として引き続き存在する。下記「保留中タスク」の英語（魔法の言葉）機能の改修要望が、時のへや転用の話なのか、この既存の別機能の改修要望なのか未確認なので、着手前にキングに確認すること。

### 「天空の城に何も表示されない」報告について
2026-09-21〜22の時点で一度報告があったが、Claude側のテスト環境では再現できず原因未特定のままだった。2026-09-22の本セッションでもヘッドレスブラウザで実際に操作して確認したが、背景・雲・トランプ・勇者とガーディアン・BGMはすべて正常に表示された。実機（スマホ）でのみ再現する可能性があり、症状が再発した場合は実機のスクリーンショットが必要。

## 既知の技術的注意点

- **JST日付処理**：`toISOString()`はUTC基準でJSTでは日付がずれる。`jstDateKey()`ヘルパーで+9時間補正すること
- **結果の永続化**：`goldMineStore = {n225: null, liquid: null}`でユニバース別保存。`stashGoldMine()`/`restoreGoldMine()`で切り替え時に退避・復元
- **深夜0:00〜4:59の扱い**：日付が変わった直後は「前日の続き」とみなす設計
- **実行モードの固定**：`runGoldMineAll()`実行時に`modeAtStart`で固定
- **判定基準日の統一**：225銘柄スキャン時、最も新しい取得日を基準日とし、古いデータの銘柄・重複コードは除外

## その他の実装済み機能

- おもちゃのピアノ：ショパン エチュードOp.10-4（CANACANA演奏mp3）の実音源再生。音量スライダーは`wd3:pianovol`に保存
- 勇者スプライトの全差し替え（白背景透過処理）：WALK(8)・JUMP(3)・CROUCH(5)・ATTACK(5)・DANCE(4)＋SKY_HERO_BACK
- 初期画面ロジック：その日最初は「しゅっぱつ準備」画面、2回目以降は勇者のハブ画面から開始（`dayData.weatherAuto`で分岐）
- 画面切り替え時、自動で最上部にスクロール

## キング（ユーザー）について

- 50歳、日本株の現物・スイングトレーダー（信用取引なし）
- Claudeを「クロ」と呼ぶ。手作業を極力させないでほしいという強い希望あり
- 監視対象銘柄・注目銘柄・判定条件など重要な項目は、クロードが勝手に決めず必ずキングに確認する
- UI変更は作る前に必ず提案して確認を得る。実装・修正前には必ず実物データを確認する
- 確認していない操作手順やUIの場所を断定して案内することを絶対禁止
- 現状は機能が多いだけで「太い幹」が足りないと感じている。遺伝子ベースの料理提案という当初の中心機能はうまくいかず今は使っていない
- 足りない太い幹：株のトレードで実際に稼げていないこと／英語が話せるようになっていないこと
- 作っている時は楽しいが作った後はすぐ飽きてしまう性質。記録・振り返り機能には情熱を持てず、「今日勝てているか」という現在の結果を重視する
- 新機能の方向性：記録機能ではなく「今日のウォッチ銘柄からAIが自動で推し銘柄を選ぶ」機能を希望。銘柄選定の根拠（使用する指数・条件、情報源、なぜ推奨するか）が明確でないと使えない
- 「金の鉱山」は日経225全銘柄を自動スキャンする方式に決定済み（小型株は対象外）

## 保留中・未着手のタスク

- 3階層フィードバックループ機能：①層同士の力関係のズレ ②情報の非対称性によるズレ ③本人の衝動的取引の癖、を記録する仕組み。③は売買ボタンを押した瞬間に🟢計画通り／🟡迷った／🔴衝動の3択でワンタップ記録する案まで合意済みだが未実装
- マクロ層（⑤）・要人発言層（⑧）のアプリ実装（`research-report.md`で情報源調査済み、未実装）
- 「テーマ人（誰が動かしているか）」探索ツール（設計案は提案済みだが「まだ作らないで」の指示あり）
- **天空の城の中身（冒険日誌＋8層レーダーチャート）**：実装対象として合意済み・再確認不要。詳細は上記「3画面の役割分担とUI方針転換」を参照
- 「アクティブな材料リスト」共通データ構造の設計・実装（勇者画面と天空の城が同じ材料を参照するための土台。上記セクション参照）
- 「使者からの伝令」手紙型UIコンポーネント（勇者画面）、天気機能のマクロ層統合（賢者モーダル含む）、金の鉱山の「気配察知」主従転換、「仲間に加える」ボタン化：いずれも上記セクションで合意済み・未実装
- 「コンプリート」→「キングクエスト」改名・UI変更：固定3項目（最強クエスト／中クエスト／楽しみクエスト）＋その他やることリスト
- 英語（魔法の言葉）機能：1セット＝会話3往復、3セットでクリア。英文/訳の表形式記録、印刷/PDF出力ボタン。**注意**：時のへやは過去問道場に転向済みのため無関係。「王様と話す」機能（天空の城の使者ポップアップ経由）の改修要望なのか要確認
- 道具に歩数計（ステッパー）機能：手入力、リストと折れ線グラフ表示
- シルバーウィーク中の投資サポートシステム本格構築（進行中）

## 関連ドキュメント

- `research-report.md`：投資サポートシステムの情報源リサーチ報告書（2026-09-18調査、8層×70URL超の検証結果、リポジトリ直下に既存）
