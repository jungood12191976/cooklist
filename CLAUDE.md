# KING THE QUEST — Claude Code project instructions

## 1. このファイルの目的
このファイルはClaude Codeが毎セッション読む「常時必要なルール」だけを置く。
詳細な現在状態・履歴・実装メモは必要なときだけ対象ファイルを読むこと。
推測で補完しない。分からないことは「分からない」と明示する。

## 2. プロジェクト概要
- アプリ名：KING THE QUEST（キング・ザ・クエスト）
- 個人用アプリ。販売目的ではない。
- 本体：単一HTML `index.html`（画像・音声をbase64埋め込み）
- 公開：GitHub Pages
- デプロイ：`main`へpush → GitHub Pages
- バージョン：`APP_BUILD`を画面下部に表示
- 最重要の世界観：RPG・冒険・成長。勉強アプリ／情報ツール化しない。
- 核：実践で得た経験・仮説・気づき・結果を「経験値」として積み上げる。

## 3. Claudeの基本行動
1. RPG・冒険・成長感を最優先。
2. 回答は短く、要点から。
3. 大きな変更は実装前にキングへ確認。
4. UI変更（タブ、配置、レイアウト、見た目）は実装前に提案し、確認を得る。
5. 重要な投資判断に関わる銘柄・条件・数値を勝手に決めない。
6. キングの手作業を最小化し、Claude自身で可能な方法を先に検討する。
7. 依頼された変更以外を勝手に改善・リファクタリングしない。

## 4. 正確性の絶対ルール
- 未確認の画面、UI、操作手順、API仕様、データを確認済みのように断定しない。
- 実装対象のデータ形式・APIレスポンス・HTML等は、可能なら実物を確認してから実装する。
- 数値は既存コード・実データ・会話内で確認済みの値を優先し、計算を検証する。
- 認証情報、APIキー、PAT、トークンをリポジトリへ保存しない。
- GitHub連携が失敗しても、1回で「権限不足」と断定しない。再確認してから事実だけを伝える。

## 5. スクリーンショット対応
スクリーンショットだけで判断できるUI問題は、まず画像を一次資料として扱う。
位置、余白、サイズ、色、z-index、transform、object-fit等が画像から判断できるなら、不要な起動・ブラウザ確認・追加スクショを行わない。
同一画面の関連修正はまとめて行う。
1回修正したら結果を提示し、依頼されていない追加改善をしない。
優先順位：
1. ユーザーのスクリーンショット
2. ユーザーの明示指示
3. 現在のコード
4. Claudeのデザイン判断

## 6. ツール・コンテキスト節約
- 必要のないファイル読み込み、ブラウザ操作、実行、スクショをしない。
- 修正対象を特定したらプロジェクト全体を読み直さない。
- 独立したツール作業は可能なら並列化する。
- 長いタスクでは、状態をファイル／gitで残し、同じ調査を繰り返さない。
- コンテキストが増えたら、必要な状態を保存したうえで `/compact` または新規コンテキストを検討する。
- 「もっと改善できそう」という理由だけで自律的な修正ループを回さない。

## 7. GitHub
- 標準デプロイはClaude Code環境からの `git push`。
- PATを手入力させたり、curlへ埋め込ませたりしない。
- 秘密情報をコミットしない。

## 8. 投資機能に関する絶対ルール
- 監視銘柄、注目銘柄、判定条件、重要パラメータをClaudeが独断で変更・追加しない。
- 日本株の情報は、実装前に実データ／公式情報を確認する。
- 「金の鉱山」は日経225を中心に扱う。投資判断に直結する条件はキングの承認なしに変更しない。
- 因果関係を断定できない情報を、断定的なアナリスト解説として表示しない。
- 事実と仮説を明確に分離する。

## 9. 現在の重要な画面方針
- 🧑‍🌾 勇者：今日の仮説を立てる場所。情報を凝縮。
- 🏰 天空の城：マクロ・振り返り。事実を詳細に表示。
- ⏰ 時のへや：株の「過去問道場」。自動問題化できる仕組みができるまで本格実装しない。
- 勇者と天空の城は同じ「アクティブな材料リスト」を参照し、要約と詳細が分断されない設計にする。
- 金の鉱山：「🔥点火済み」より「🔮気配察知」を主役にする方向で確定。
- 適時開示監視銘柄は「仲間に加える」導線を優先。
- マクロ環境は「天気」に統合する方向で確定。

## 10. 実装済み／保留の詳細
詳細な実装状況、関数名、保存キー、API経路、既知の技術的注意点、未着手タスクは、
必要なときに `PROJECT_STATE.md` を読むこと。
過去の経緯を毎セッション最初から読み直さない。

## 11. 重要な設計思想
- 「記録するためのアプリ」ではなく、「今日の行動・判断を良くするアプリ」を目指す。
- 機能を増やすことより、太い幹（トレードの実践・判断・利益につながる仕組み）を優先。
- 手動入力を増やさない。自動取得・自動整理・自動フィードバックを優先。

## 12. 作業開始時の最小手順
1. `pwd`
2. 依頼内容に直接関係するファイルだけ確認
3. 必要なら `PROJECT_STATE.md` の該当部分だけ読む
4. 変更対象と変更内容を短く提示（複数ファイル・UI変更の場合）
5. 実装
6. 必要最小限の検証
7. 依頼された範囲で終了
